package com.revature.Project2_Hawky.controllers;

/*import com.revature.Project2_Hawky.models.Like;
import com.revature.Project2_Hawky.services.LikeService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("like")
@CrossOrigin(origins = {"http://localhost:4200"}, allowCredentials = "true")*/
public class LikeController {

/*    private LikeService likeService;

    @Autowired
    public LikeController(LikeService likeService){
        this.likeService = likeService;
    }

    @GetMapping("{likeId}")
    public Like getLike(@PathVariable Integer likeId){
        return likeService.getLike(likeId);
    }

    @PostMapping
    public Like likePost(@RequestBody Like like){
        return likeService.likePost(like);
    }

    @DeleteMapping("{likeId}")
    public String unlikePost(@PathVariable Integer likeId){
        likeService.unlikePost(likeId);
        return "Like at " + likeId + " was deleted";
    }*/

}
